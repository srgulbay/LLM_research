class ExampleService {
    // Örnek veri işlemleri ve diğer hizmetlerle ilgili yöntemler burada tanımlanacak.

    public getData(): string {
        // Veriyi almak için bir yöntem
        return "Veri alındı";
    }

    public processData(data: any): any {
        // Veriyi işlemek için bir yöntem
        // İşlem yapılacak veri üzerinde gerekli işlemler burada gerçekleştirilecek
        return data; // İşlenmiş veri döndürülüyor
    }
}

export default ExampleService;